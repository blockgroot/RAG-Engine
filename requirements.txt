openai>=1.40.0
python-dotenv>=1.0.0
sentence-transformers>=3.0.0
psycopg[binary]>=3.1.0
psycopg-pool>=3.2.0
pgvector>=0.3.0
notion-client>=2.2.1
ddgs>=6.0.0
cryptography>=42.0.0
httpx>=0.27.0
pyjwt>=2.8.0
# CLI presentation only (scripts/cli.py) — never imported by the app/ core runtime.
rich>=13.0.0
rank-bm25>=0.2.2

# --- HTTP API layer (app/api/) — Phase 13 ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

langgraph==0.2.74

# --- dev / test ---
pytest>=8.0.0
# (httpx above also serves as FastAPI's TestClient transport)
symspellpy>=6.7,<7
